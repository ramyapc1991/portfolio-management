public class TestExpression {

    public static void main(String[] args) {
        String expr ="2*3+4*2";

        System.out.println(getExpressionValue(expr));
    }

    private static int getExpressionValue(String expr) {
//        // split if + present and conti.
//        String[] arr = expr.split("\\+");
//        //for sum = sum +getRideOfMulti
//        String exprAdd = getRideOfMulti(arr[i]);
//        System.out.println(exprAdd);
//
//        //else
//        // check for * in expre then proceed.
//        // split by *
//        // calculate and update sum based on the number of splited array.
//        return 0;
//    }
//
//    private static int getRideOfMulti(String expr) {
//        if(expr.contains("*")){
//            String[] arr = expr.split("\\*");
//            return arr[0] * arr[1];
//        }
//
////        String str ="";
////        int i =0;
////        while(i < expr[0].length()){
////            if(String.valueOf(expr[i].charAt(0)).equals("*")){
////                int sum = Integer.parseInt(String.valueOf(expr[i].charAt(i-1))) * Integer.parseInt(String.valueOf(expr[i].charAt(i+1)));
////                str = str + sum;
////                i = i+2;
////            }
////        }
//
//    }
        return 0;
    }
}
